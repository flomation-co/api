# Test fixture config files contain dummy values, not real secrets
internal/config/testdata/

# Backfill cmd unit tests embed JSON fixtures with fake bot_token/signing_secret keys
cmd/backfill_channel_secrets/main_test.go
